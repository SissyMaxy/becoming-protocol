-- 622: machine-overseer integration — Mommy as the brain of the edging/milking rig.
-- Applied 2026-06-29 via Management API; recorded here.
-- machine_programs (Mommy-authored session programs), machine_sessions (the log),
-- machine_events (orgasm/denial/overstim/struggle). All RLS auth.uid()=user_id.
-- The overseer edge fn enforces safeword + HR dead-man hard-stops (complements the
-- physical kill switches, never replaces them).
-- (See supabase/functions/machine-overseer/index.ts for the full DDL applied.)
SELECT 1;
