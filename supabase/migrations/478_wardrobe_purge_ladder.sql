-- 478 — Wardrobe purge ladder.
-- 7-category one-for-one masc → fem replacement. Different from
-- mig 447 wardrobe-prescription (wear-rotation). This REPLACES the
-- closet contents permanently.
-- Phases: socks → underwear → shirts_pants → sleepwear → shoes →
-- outerwear → identifying_items. 2x monthly cron (1st + 15th 15:00 UTC).

-- (Schema + ladder seed + eval function applied via DB.)
