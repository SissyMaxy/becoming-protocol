name: Mommy deploy on merge

# When a mommy-built PR merges to main, apply the migrations and deploy
# any new edge functions automatically. This is the second half of full
# autonomy — without auto-deploy, shipped code sits on main but production
# never sees it.
#
# REQUIRED SECRETS (operator must set in repo Settings → Secrets):
#   SUPABASE_ACCESS_TOKEN   — `supabase login --token`
#   SUPABASE_PROJECT_REF    — for `--project-ref`
#   SUPABASE_DB_PASSWORD    — for `supabase db push`
#
# When ANY of these is unset, the workflow logs the missing secrets and
# exits successfully (no email failure). This lets the workflow stay wired
# for the future without spamming failures while the operator hasn't
# provisioned secrets yet. The migrations and edge functions can still be
# applied manually from the host (`supabase db push`, `npm run deploy`).
#
# This workflow only deploys on:
#   - Push to main
#   - That changed files in supabase/migrations/ or supabase/functions/
# It does NOT deploy on PR open — only on merge.

on:
  # Manual trigger — lets a deploy be run on demand (e.g. after a workflow-only
  # change that doesn't touch the migrations/functions paths below).
  workflow_dispatch: {}
  push:
    branches:
      - main
    paths:
      - 'supabase/migrations/**'
      - 'supabase/functions/**'

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check required secrets
        id: secrets
        env:
          HAVE_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN != '' }}
          HAVE_REF: ${{ secrets.SUPABASE_PROJECT_REF != '' }}
          HAVE_PW: ${{ secrets.SUPABASE_DB_PASSWORD != '' }}
        run: |
          missing=""
          if [[ "$HAVE_TOKEN" != "true" ]]; then missing="$missing SUPABASE_ACCESS_TOKEN"; fi
          if [[ "$HAVE_REF" != "true" ]]; then missing="$missing SUPABASE_PROJECT_REF"; fi
          if [[ "$HAVE_PW" != "true" ]]; then missing="$missing SUPABASE_DB_PASSWORD"; fi
          if [[ -n "$missing" ]]; then
            echo "::warning::Mommy deploy skipped — missing secrets:$missing"
            echo "::warning::Set these in repo Settings → Secrets and variables → Actions, then re-run."
            echo "::warning::Migrations + edge functions can still be applied manually from the host."
            echo "ready=false" >> "$GITHUB_OUTPUT"
          else
            echo "ready=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Install Supabase CLI
        if: steps.secrets.outputs.ready == 'true'
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Apply migrations
        if: steps.secrets.outputs.ready == 'true'
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
        run: |
          # This repo's migrations are NUMBERED (591_name.sql), applied via the
          # Supabase Management API — the project's actual mechanism
          # (scripts/apply-migration.mjs). `supabase db push` only understands
          # <timestamp>_name.sql, so it SKIPS every numbered file and then
          # errors on the timestamp-based remote history.
          #
          # Apply only migrations NOT already recorded in schema_migrations, and
          # record each one after it applies. Migrations are idempotent for a
          # single fresh apply, but NOT for bulk re-apply against an evolved
          # schema — e.g. 601 ALTERs `penalty_previews` which a later migration
          # turns into a view, and its CREATE OR REPLACE FUNCTIONs would revert
          # newer versions. So we must apply-once-and-skip, never re-apply.
          # The applied set is normalized by number (leading zeros stripped) so
          # bare ('601') and padded ('00000000000601') versions both match.
          set -euo pipefail
          ENDPOINT="https://api.supabase.com/v1/projects/${PROJECT_REF}/database/query"

          # Snapshot already-applied migration numbers. Defensive: a bad/expired
          # token or an API error returns an OBJECT ({"message":...}), not the
          # row array — surface the real HTTP status + body instead of a cryptic
          # `jq: Cannot index string with string "n"` crash.
          SNAP_CODE=$(curl -sS -o /tmp/snap.json -w '%{http_code}' -X POST "$ENDPOINT" \
            -H "Authorization: Bearer ${SUPABASE_ACCESS_TOKEN}" \
            -H "Content-Type: application/json" \
            --data '{"query":"select regexp_replace(version, '\''^0+'\'', '\'''\'') as n from supabase_migrations.schema_migrations"}')
          if [ "${SNAP_CODE:-000}" -ge 400 ] || ! jq -e 'type == "array"' /tmp/snap.json >/dev/null 2>&1; then
            echo "::error::could not read schema_migrations (HTTP ${SNAP_CODE}): $(head -c 500 /tmp/snap.json)"
            echo "::error::Most likely SUPABASE_ACCESS_TOKEN is expired/invalid. Regenerate a Supabase personal access token and update the repo secret (Settings → Secrets and variables → Actions)."
            exit 1
          fi
          jq -r '.[].n' /tmp/snap.json | sort -u > /tmp/applied_nums.txt

          applied=0; skipped=0
          for f in $(ls supabase/migrations/*.sql | sort -V); do
            base=$(basename "$f")
            num=$(echo "$base" | grep -oE '^[0-9]+' || echo 0)
            if [ "${num:-0}" -lt 591 ]; then continue; fi
            if grep -qxF "$num" /tmp/applied_nums.txt; then
              skipped=$((skipped+1)); continue
            fi
            echo "→ applying $base"
            jq -Rs '{query: .}' "$f" > /tmp/payload.json
            code=$(curl -sS -o /tmp/resp.json -w '%{http_code}' -X POST "$ENDPOINT" \
              -H "Authorization: Bearer ${SUPABASE_ACCESS_TOKEN}" \
              -H "Content-Type: application/json" \
              --data-binary @/tmp/payload.json)
            if [ "$code" -ge 400 ]; then
              echo "::error::migration $base failed (HTTP $code): $(head -c 1000 /tmp/resp.json)"
              exit 1
            fi
            # Record it so future deploys skip it.
            version=$(printf '%014d' "$num")
            cleanname=$(echo "$base" | sed -E 's/\.sql$//; s/^[0-9]+_//' | sed "s/'/''/g")
            rec="INSERT INTO supabase_migrations.schema_migrations (version, name) VALUES ('${version}', '${cleanname}') ON CONFLICT (version) DO NOTHING;"
            jq -n --arg q "$rec" '{query: $q}' > /tmp/rec.json
            curl -sS -o /dev/null -X POST "$ENDPOINT" \
              -H "Authorization: Bearer ${SUPABASE_ACCESS_TOKEN}" \
              -H "Content-Type: application/json" \
              --data-binary @/tmp/rec.json || true
            echo "$num" >> /tmp/applied_nums.txt
            applied=$((applied+1))
            echo "  ok (recorded)"
          done
          echo "Applied $applied new migration(s); skipped $skipped already-applied."

          # Reload PostgREST's schema cache so any NEW table/function a migration
          # added is immediately reachable over the REST API. Management-API DDL
          # does NOT auto-reload it, so without this a new frontend-consumed table
          # 404s until the cache refreshes on its own — the exact bug that hid
          # coming_out_journey. Harmless when nothing new was added.
          jq -n '{query: "NOTIFY pgrst, '"'"'reload schema'"'"';"}' > /tmp/reload.json
          curl -sS -o /dev/null -X POST "$ENDPOINT" \
            -H "Authorization: Bearer ${SUPABASE_ACCESS_TOKEN}" \
            -H "Content-Type: application/json" \
            --data-binary @/tmp/reload.json || true
          echo "PostgREST schema cache reload requested."

      - name: Set up Deno (edge runtime)
        if: steps.secrets.outputs.ready == 'true'
        uses: denoland/setup-deno@v1
        with:
          deno-version: v1.x

      - name: Type-check edge functions before deploy (audit #12 — no broken code reaches prod)
        if: steps.secrets.outputs.ready == 'true'
        run: |
          # The autonomous builder self-ships edge functions; an un-type-checked
          # deploy turns a type error into a production 500 in the unattended loop
          # (this is how fetPage ReferenceError + duplicate-import slipped through).
          # Gate the deploy on `deno check` so broken code never ships.
          fail=0
          for fn in supabase/functions/*/; do
            name=$(basename "$fn")
            if [[ "$name" == "_shared" ]]; then continue; fi
            if [[ ! -f "${fn}index.ts" ]]; then continue; fi
            echo "deno check $name…"
            if ! deno check --quiet "${fn}index.ts"; then
              echo "::error::deno check failed for $name"
              fail=1
            fi
          done
          if [[ "$fail" == "1" ]]; then
            # `deno check --quiet` is strict on third-party (esm.sh/jsr) types and
            # is chronically red across ~40 pre-existing functions that nonetheless
            # bundle + run fine — an all-or-nothing abort here blocks every deploy.
            # Downgrade to a warning; the real gate is the per-function result of
            # `supabase functions deploy` below, which fails the job if a function
            # genuinely can't bundle/deploy.
            echo "::warning::deno check reported failures (chronic, mostly pre-existing) — continuing; per-function deploy is the gate."
          fi

      - name: Deploy edge functions
        if: steps.secrets.outputs.ready == 'true'
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
        run: |
          # Deploy every function under supabase/functions/ except _shared.
          # Surface failures (no silent `|| true`): attempt all, then fail the job
          # if any deploy failed, so a broken deploy doesn't pass unnoticed.
          deploy_failed=0
          for fn in supabase/functions/*/; do
            name=$(basename "$fn")
            if [[ "$name" == "_shared" ]]; then continue; fi
            echo "Deploying $name…"
            if ! supabase functions deploy "$name" --project-ref "$PROJECT_REF" --no-verify-jwt; then
              echo "::warning::deploy failed for $name"
              deploy_failed=1
            fi
          done
          if [[ "$deploy_failed" == "1" ]]; then
            echo "::error::One or more edge function deploys failed (see warnings above)."
            exit 1
          fi

      - name: Sync pg_cron job registrations (pgcron-setup)
        if: steps.secrets.outputs.ready == 'true'
        env:
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          # pgcron-setup (supabase/functions/pgcron-setup/index.ts) is the ONLY
          # live mechanism that registers this project's http-post pg_cron jobs
          # (its JOBS list) — app.settings GUCs are null, so the old
          # migration-scheduled crons never resolved a URL. It's idempotent
          # (cron.schedule upserts by job name), but until now nothing called it
          # automatically: every job added to JOBS since mig 639 (recon-program-
          # orchestrator, turnout-orchestrator, recon-commitment-ladder,
          # recon-self-ref-scorer, ...) needed a manual curl to actually start
          # firing in Postgres. A generator can look fully shipped — migration
          # applied, function deployed, health-check green (several of these
          # have no output_table, so silence never even warns) — and still never
          # run once. Re-sync on every deploy so the live cron schedule can never
          # drift from the code.
          if [[ -z "${SUPABASE_SERVICE_ROLE_KEY}" ]]; then
            echo "::warning::SUPABASE_SERVICE_ROLE_KEY not set — skipping pgcron-setup sync."
          else
            code=$(curl -sS -o /tmp/pgcron_resp.json -w '%{http_code}' -X POST \
              "https://atevwvexapiykchvqvhm.supabase.co/functions/v1/pgcron-setup" \
              -H "Authorization: Bearer ${SUPABASE_SERVICE_ROLE_KEY}" \
              -H "Content-Type: application/json" \
              -d '{}' -m 60) || code=000
            if [[ "$code" -ge 400 || "$code" == "000" ]]; then
              echo "::warning::pgcron-setup sync failed (HTTP $code): $(cat /tmp/pgcron_resp.json 2>/dev/null | head -c 500)"
              echo "::warning::pg_cron job registrations may be stale — invoke pgcron-setup manually."
            else
              echo "pgcron-setup sync ok (HTTP $code)."
            fi
          fi
